{{>partials/header}}
{{>partials/navbar}}


<div class="container">
    <hr>
    <div class="card">
        <div class="card-header">
            <h2>Editar categoria</h2>
        </div>
        <div class="card-body">
            <form method="POST" action="/admin/categories/update">
                <input type="text" name="title" placeholder="Defina o título da categoria"
                    value="{{category.title}}" class="form-control" /><br>
                <input type="hidden" name="id" value="{{category.id}}">
                <button class="btn btn-success">Salvar</button>
            </form>
        </div>

    </div>
</div>

{{>partials/footer}}